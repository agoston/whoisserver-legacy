#
# Table structure for table 'irt'
#
CREATE TABLE irt (
  thread_id int(11) DEFAULT '0' NOT NULL,
  object_id int(10) unsigned DEFAULT '0' NOT NULL,
  irt varchar(80) DEFAULT '0' NOT NULL,
  dummy tinyint(4) DEFAULT '0' NOT NULL,
  PRIMARY KEY (object_id)
);

alter table irt add index (irt(25));

#
# Table structure for table 'mnt_irt'
#
CREATE TABLE mnt_irt (
  thread_id int(11) DEFAULT '0' NOT NULL,
  object_id int(10) unsigned DEFAULT '0' NOT NULL,
  irt_id int(10) unsigned DEFAULT '0' NOT NULL,
  object_type tinyint(3) unsigned DEFAULT '0' NOT NULL,
  PRIMARY KEY (irt_id,object_id)
);

alter table mnt_irt add index (object_id);
alter table mnt_irt add index (object_type);

#
# Table structure for table 'irt_nfy'
#
CREATE TABLE irt_nfy (
  thread_id int(11) DEFAULT '0' NOT NULL,
  object_id int(10) unsigned DEFAULT '0' NOT NULL,
  irt_nfy varchar(80) DEFAULT '' NOT NULL,
  PRIMARY KEY (irt_nfy(80), object_id)
);

alter table irt_nfy add index (object_id);

INSERT INTO object_order VALUES (17,41);
