object: all
lines: single
body: ack_sum_found(1)
body: ack_sum_ok(0, 0, 0, 0, 0)
body: ack_sum_fail(1, 0, 0, 1, 0)

object: person TP2-DB-TEST
body: delete_failed
body: syntax_ok
body: obj_class(ack, person, test person2)
body: obj_error(ack, This does not exactly match the object stored in the database.)
body: no_obj_warn(ack)

object: person TP2-DB-TEST
lines: single
body: count_error(ack, 1,2)
