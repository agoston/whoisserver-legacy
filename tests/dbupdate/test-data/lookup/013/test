# radix lookup tests
#
# CASE 13: default route query for route
#
# default query for non-existing route
# using ipv4 prefix
#
#SUCCESS
#
# RESULT 13: Closest encompassing routes returned

$EXEC_BEFORE=$DATADIR/lookup/reload.sh DB-TEST

$EXEC_AFTER=$DATADIR/lookup/query.pl -- -T route 20.13.100.0/24

