# radix lookup tests
#
# CASE 96: send a query with no search key
#
#
#SUCCESS
#
# RESULT 96: No search key provided

$EXEC_BEFORE=$DATADIR/lookup/reload.sh DB-TEST

$EXEC_AFTER=$DATADIR/lookup/query.pl -- -T route

