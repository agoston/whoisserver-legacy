# radix lookup tests
#
# CASE 15: default route query for route using ipv6 prefix
#
# default query for route
# using ipv6 prefix
#
#SUCCESS
#
# RESULT 15: No entries found

$EXEC_BEFORE=$DATADIR/lookup/reload.sh DB-TEST

$EXEC_AFTER=$DATADIR/lookup/query.pl -- -T route 2001:658:21A::/48

