# radix lookup tests
#
# CASE 16: default route6 query for route
#
# default query for existing route
# using ipv6 prefix
#
#SUCCESS
#
# RESULT 16: All expected objects returned

$EXEC_BEFORE=$DATADIR/lookup/reload.sh DB-TEST

$EXEC_AFTER=$DATADIR/lookup/query.pl -- -T route6 2001:658:21A::/48

