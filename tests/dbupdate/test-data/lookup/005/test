# radix lookup tests
#
# CASE 5: single less-specific query using ipv4 address
#
# single less-specific query for inetnum
# using ipv4 address
#
#SUCCESS
#
# RESULT 5: All expected objects returned

$EXEC_BEFORE=$DATADIR/lookup/reload.sh DB-TEST

$EXEC_AFTER=$DATADIR/lookup/query.pl -- -l -T inetnum 192.168.201.145

